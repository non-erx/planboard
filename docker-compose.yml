services:
  planboard:
    build: .
    image: nonerx/planboard:latest
    restart: unless-stopped
    ports:
      - "80:80"
    volumes:
      - db-data:/app/data
    environment:
      - DATABASE_URL=sqlite:////app/data/planboard.db

volumes:
  db-data:
